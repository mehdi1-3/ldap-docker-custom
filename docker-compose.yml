version: '3'

services: 

  ldap-server:
    image: 'ldap:v33'
    environment:
      - "ROOT_DN_PW=secret"
      - "ROOT_DN=cn=Manager"
    volumes:
      - '/home/admin/newldap1/var:/var/lib/openldap/openldap-data'
      #- './conf/:/etc/openldap' 
      #- './certificates:/certificates'